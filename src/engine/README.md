Use this folder for the local bridge to the Java engine.

Typical responsibilities:

- compile or invoke the upstream engine
- launch matches with specific seeds and bot commands
- collect stdout/stderr and parse results
- normalize match outcomes into a stable TypeScript interface
